export type createCloseStatus = {
    codeNumber: string;
    processor: string;
    isClosed: string;
  };
  
  export type getCloseStatus = {
    codeNumber: string;
    processor: string;
    isClosed: string;
  };
  
