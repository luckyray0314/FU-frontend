export type createCloseStatusAdult = {
    codeNumber: string;
    processor: string;
    isClosed: string;
  };
  
  export type getCloseStatusAdult = {
    codeNumber: string;
    processor: string;
    isClosed: string;
  };
  
